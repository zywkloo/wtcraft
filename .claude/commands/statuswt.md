You are checking active wtcraft worktree tasks.

Run:

```bash
wtcraft status
```

Explain the joined task-specification and sidecar-state facts clearly. If the
command is unavailable, read lifecycle/results from `.worktree-state.json` and
stable task metadata from `.worktree-task.md`, falling back to legacy Markdown
frontmatter only when no sidecar exists.
