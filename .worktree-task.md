---
branch: claude/gemini-support-worktree-k9Mqs
agent: claude
status: ready
created: 2026-05-28
priority: medium
base: main
---

## Scope

- `templates/.agent-harness/executor.md`
- `README.md`

## Steps

- [ ] [D] Read both files in Scope in full before editing.
- [ ] [A] In `templates/.agent-harness/executor.md`, add a Gemini CLI row to the Model Selection table (after the Codex row), and add a **Gemini fallback** note below the Codex fallback note explaining which model flags to use and that Gemini CLI reads `GEMINI.md` for project context.
- [ ] [A] In `README.md`, add a Gemini CLI row to the Requirements table (after the Claude + Codex row) marking it as optional, and update the prose below the table to mention Gemini as a supported executor.
- [ ] [D] Run verification checks and capture outcomes.

## Off-limits

- `scripts/wtcraft`
- `src/`
- `tests/`
- `pyproject.toml`
- `package.json`
- `CLAUDE.md`
- `AGENTS.md`
- `.agent-harness/`
- `templates/worktrees/`
- `templates/.claude/`

## Context

The Gemini CLI (https://github.com/google-gemini/gemini-cli) is invoked as
`gemini` from the terminal. Its project-context file is `GEMINI.md` (analogous
to `CLAUDE.md` for Claude Code and `AGENTS.md` for Codex). The default model
is `gemini-2.5-pro`; a cheaper fallback is `gemini-2.5-flash` (pass
`--model gemini-2.5-flash`). Do not invent flags — stick to `--model`.

The executor.md table currently has two rows (Claude Code, Codex CLI) plus an
"Other / unknown" catch-all. Insert Gemini CLI between Codex and the catch-all.

The README Requirements table row for agent access currently reads:
  | Claude + Codex access | yes (for full workflow) | both are expected... |
Add a new row beneath it:
  | Gemini CLI access | no (optional executor) | free tier available; use as an executor agent when Codex quota is exhausted |
Also update the prose paragraph ("For the full multi-agent workflow…") to
mention Gemini as an optional third executor alongside Claude and Codex.

## Verification

- [ ] `grep -n "Gemini CLI" templates/.agent-harness/executor.md`
- [ ] `grep -n "gemini-2.5" templates/.agent-harness/executor.md`
- [ ] `grep -c "Gemini" README.md`
- [ ] `grep -n "Gemini CLI" README.md`
